export default class ClassRoom {
  // create constructor instantiate the attribute maxStudentsSize
  constructor(maxStudentsSize){
    // attribute gets assigned to property name with "_"
    this._maxStudentsSize = maxStudentsSize;
  }
}
